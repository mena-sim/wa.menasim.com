# Keep shell scripts and server config LF so they run on the Linux VPS
# (CRLF would cause "bad interpreter: /usr/bin/env bash^M" errors).
*.sh            text eol=lf
*.service       text eol=lf
*.conf          text eol=lf
deploy/**       text eol=lf
