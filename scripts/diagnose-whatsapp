#!/usr/bin/env bash
# Run WhatsApp diagnostics using the app virtualenv (no need to activate it).
set -euo pipefail
cd "$(dirname "$0")/.."
if [ ! -x ".venv/bin/python3" ]; then
  echo "Missing .venv — run ./deploy.sh first." >&2
  exit 1
fi
exec .venv/bin/python3 scripts/diagnose_whatsapp.py "$@"
